name: stocker-ai
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - fastapi
      - "uvicorn[standard]"
      - pydantic
      - pandas
      - joblib
      - scikit-learn
      - pygam
      - statsmodels
      - matplotlib
      - pathlib